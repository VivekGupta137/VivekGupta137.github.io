import { motion } from "framer-motion";
import OkIcon from "../icons/okIcon";

const AboutContactEmailSent = (props) => {
    return (
        <motion.div {...props}>
            <OkIcon />
        </motion.div>
    );
};

export default AboutContactEmailSent;
